# ── 필수 ──
NEXTAUTH_SECRET=          # openssl rand -base64 32 로 생성

# ── 선택 ──
API_KEYS=                 # 쉼표 구분. 예: sk-mykey1,admin:sk-mykey2
PORT=8080                 # 호스트 포트 (기본 8080)
POOL_SIZE=1               # 워커 풀 크기
CLAUDE_MODEL=claude-sonnet-4-6
